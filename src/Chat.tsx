import React, { ReactElement } from 'react';
import "./Chat.css";

interface Props {
    
}

function Chat({}: Props): ReactElement {
    return (
        <div className="chat">
            <h1>Chat window</h1>
        </div>
    )
}

export default Chat
